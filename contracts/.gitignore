# Foundry build artifacts
cache/
out/

# Dependencies — installed via `forge install`
lib/

# Broadcast run logs (keep chain-specific logs, ignore local/dry-run)
broadcast/*/31337/
broadcast/**/dry-run/
